# USE employees;
# SELECT * employees WHERE (first_name = 'Irena' OR 'Vidya' OR 'Maya') ORDER BY first_name;
# SELECT * employees WHERE (first_name = 'Irena' OR 'Vidya' OR 'Maya') ORDER BY first_name, last_name;
# SELECT * employees WHERE (first_name = 'Irena' OR 'Vidya' OR 'Maya') ORDER BY last_name, first_name;
# SELECT * employees WHERE last_name LIKE 'E%' OR '%E' ORDER BY emp_no;
# SELECT * employees WHERE last_name LIKE 'E%' OR '%E' ORDER BY emp_num DESC;
# SELECT * employees WHERE first_name IN 'Irena','Vidya','Maya' ORDER BY last_name, first_name;
